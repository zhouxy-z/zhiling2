syntax = "proto3";
option go_package = "plants_server/proto/base";
package base;

message DeckAttrs {
  string id = 1; // 卡组ID
  string name = 2; // 卡组名称
  int32 type = 3; // 卡组类型，只读属性
  bool is_default = 4; // 是否默认，只读属性
}
